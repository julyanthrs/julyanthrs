# Optional. JSON endpoint that receives { name, email, projectType, message }.
# Without it, the contact form opens a pre-filled email in the visitor's mail app.
# Your endpoint must validate and rate-limit on the server.
VITE_CONTACT_ENDPOINT=
