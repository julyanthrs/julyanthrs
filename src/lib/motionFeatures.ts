/**
 * framer-motion feature bundle, split into its own chunk and loaded by <LazyMotion>.
 * domMax (rather than domAnimation) because the site uses drag and shared layout animations.
 */
export { domMax as default } from "framer-motion";
