# Optional. JSON POST endpoint for the contact form (e.g. Formspree, your own API).
# When unset, the form falls back to opening the visitor's email client.
VITE_CONTACT_ENDPOINT=
# Address used by the mailto fallback.
VITE_CONTACT_EMAIL=hello@kaimoreno.dev
